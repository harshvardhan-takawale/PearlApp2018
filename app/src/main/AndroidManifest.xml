<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.dota.pearl18.pearlapp2018">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- TODO: Update API Key (Manifest)-->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyB1XaiMjb3mPHnAG5JR3urnGF6-jbzQecc"/>
        <activity
            android:name=".Articles.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.ContactsActivity"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".guide.GuideActivity"/>
        <activity android:name=".guide.MapsActivity"/>
        <activity android:name=".Articles.TextDisplayActivity"/>
        <activity android:name=".activities.CreditsActivity"
            android:label="App Credits">
        </activity>
    </application>

</manifest>